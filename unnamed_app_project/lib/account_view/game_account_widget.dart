import 'package:flutter/material.dart';
import 'package:unnamed_app_project/objects/game_accounts/game_account.dart';

class GameAccountWidget extends StatelessWidget {
  final GameAccount gameAccount;
  const GameAccountWidget({@required this.gameAccount});
  @override
  Widget build(BuildContext context) {
    // TODO: create this widget. Assume GameAccount fields are not null
    return Container();
  }
}
